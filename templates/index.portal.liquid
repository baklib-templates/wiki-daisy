{% assign default_hero_image = 'images/hero.jpg' | asset_url %}
{% assign hot_tags = page.settings.hot_tags | split: ',' %}

<!-- Hero Section -->
<section id="hero" style="background-image: url(https://img.daisyui.com/images/stock/photo-1507358522600-9f71e620c44e.webp);" class="hero flex items-center justify-center flex-col bg-no-repeat bg-cover bg-center {% if page.settings.hero_image_url %} bg-base-200 {% else %} bg-base-300 {% endif %} h-[250px] md:h-[350px]"
  {% if page.settings.hero_image_url %} style="background-image: url({{ page.settings.hero_image_url | default: default_hero_image }})" {% endif %}
  >
  <div class="flex items-center justify-center flex-1 w-full py-3">
    <div class="container flex flex-col h-full px-4 mx-auto">
      <div class="flex flex-col items-center justify-center flex-grow">
        <h1 class="py-2 mt-12 text-2xl md:text-4xl font-bold transition duration-500 md:mt-0 text-shadow">
          {{ page.settings.title }}
        </h1>
        <p class="py-2 text-base transition duration-500 text-shadow">{{ page.settings.description }}</p>

        <div class="z-0 flex flex-col items-center justify-center w-full md:w-2/3">
          {% form_tag 'search', class: 'w-full' %}
            <div class="relative rounded-2xl shadow-sm">
              <button type="submit" class="absolute inset-y-0 right-0 flex items-center pl-4 pr-3 sm:right-auto">
                <svg xmlns="http://www.w3.org/2000/svg" class="w-5 h-5 text-primary" fill="none" viewBox="0 0 24 24" stroke="currentColor" > <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" /> </svg>
              </button>
              <input
                type="text"
                name="{{form.keywords_field_name}}" value="{{ search.keywords | escape_once }}"
                placeholder="{{ "placeholders.search" | t }}"
                autocomplete="off"
                class="block w-full h-10 md:h-12 lg:h-14 pl-3 pr-10 transition duration-300 ease-in-out bg-accent-100 bg-opacity-90 border-primary border-2 rounded outline-none focus:ring-0 focus:border-primary focus:border-4 sm:pl-12 sm:text-lg focus:bg-opacity-90"
              >
            </div>
          {% endform_tag %}

            <div class="items-center justify-start hidden w-full mt-4 md:flex">
              <p class="mr-3 text-primary">{{ 'generic.popular_search' | t }}:</p>
              {% for search_hot_key in hot_tags %}
                <a
                  href="/-/search?q={{ search_hot_key }}"
                  class="mr-3 hover:underline focus:ring"
                >
                  {{ search_hot_key }}
                </a>
              {% endfor %}
            </div>

        </div>
      </div>
    </div>
  </div>
</section>

<div class="max-w-7xl mx-auto">

  <div class="diff aspect-[16/9] my-12 lg:my-20">
    <div class="diff-item-1">
      <div class="bg-secondary text-primary-content text-9xl font-black grid place-content-center">DAISY</div>
    </div>
    <div class="diff-item-2">
      <div class="bg-primary text-9xl font-black grid place-content-center">WIKI</div>
    </div>
    <div class="diff-resizer"></div>
  </div>

  <div class="grid grid-cols-1 lg:grid-cols-2 gap-4 my-12">
    <article class="prose border-r">
      <h1 class="text-secondary">什么是 Daisy UI?</h1>
      <p>
        DaisyUI 是流行 CSS 框架 Tailwind CSS 的时尚实用插件。
        美观性和功能性需要齐头并进，才能为用户创造引人入胜且高效的体验，DaisyUI 刚好做到了这一点。
      </p>
      <p>
        TailwindCSS 是一个高度可定制的低级 CSS 框架。DaisyUI 的作用是提供一组丰富的预构建组件，可以使用 TailwindCSS 在项目中轻松集成和自定义这些组件。
        这意味着您可以享受 Tailwind 提供的所有自定义优势，同时享受更传统的 UI 套件的速度和简便性。
      </p>
      <!-- ... -->
    </article>
    <article class="prose">
      <h1 class="text-primary">什么是 Daisy Wiki?</h1>
      <p>
        Daisy Wiki 是通过 DaisyUI 打造的一个 Wiki 站点模板，如果你希望创建一个内容驱动型的网站，又希望前端具有更加现代化的用户体验，则选择这个模板就好了。
      </p>

      <p>
        操作方式很简单，你只需要在工作台 1）创建一个知识库；2）在[模板市场]中选择[Daisy Wiki]模板；3）点击[安装]，三步操作完成，一个崭新的 Wiki站点就呈现在你面前了。
        模板预设了 10+主题切换，以及知识库界面包括树状菜单、文档排版、页面导航等。
      </p>
      <!-- ... -->
    </article>
  </div>

</div>


{% schema %}
  {
    "name": "Portal",
    "description": "Ideal for feature-rich portal site",
    "thumb_url": "images/theme/index-portal.png",
    "preview_image_urls": [
      "images/theme/index-portal.png"
    ],
    "sub_page_templates": ["page"],
    "settings": [
        {
          "id": "hero_image_url",
          "type": "image_picker",
          "ratio": "19:6",
          "width": 900,
          "label": "t:settings_schema.generic.settings.hero_image_url.label",
          "info": "t:settings_schema.generic.settings.hero_image_url.info"
        },
        {
          "id": "title",
          "type": "text",
          "label": "t:settings_schema.generic.settings.title.label",
          "default": "Knowledge base site"
        },
        {
          "id": "description",
          "type": "text",
          "label": "t:settings_schema.generic.settings.description.label",
          "default": "👋 What Can We Help?"
        },
        {
          "id": "hot_tags",
          "label": "t:settings_schema.generic.settings.hot_tags.label",
          "type": "text",
          "placeholder": "sign in,install,account,how to"
        },
        {
          "id": "index_card_page_count",
          "type": "number",
          "min": 1,
          "max": 20,
          "step": 1,
          "default": 5,
          "label": "首页列表展示页面数量",
          "info": ""
        }
      ]
  }
{% endschema %}
